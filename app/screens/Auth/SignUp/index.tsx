import React from 'react';
import { Text, View } from 'react-native';
import { INavigation } from '../../../types';

const SignUp = ({ navigation }: { navigation: INavigation }) => {
  return (
    <View>
      <Text>Hello, this is signup!</Text>
    </View>
  );
};

export default SignUp;
